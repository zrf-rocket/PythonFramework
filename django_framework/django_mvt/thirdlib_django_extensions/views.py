import hashlib
import os.path

os.path.join()
os.path.dirname()
os.path.basename()
os.path.exists()
os.path.isfile()
os.path.isdir()

hashlib.sha1()
hashlib.md5()
# bcrypt.hashpw()


from django.shortcuts import render

# Create your views here.
from django.utils import (
    archive,
    asyncio,
    autoreload,
    baseconv,
    cache,
    connection,
    crypto,
    datastructures,
    dateformat,
    dateparse,
    dates,
    datetime_safe,
    deconstruct,
    decorators,
    deprecation,
    duration,
    encoding,
    feedgenerator,
    formats,
    functional,
    hashable,
    html,
    http,
    inspect,
    ipv6,
    itercompat,
    jslex,
    log,
    lorem_ipsum,
    module_loading,
    numberformat,
    regex_helper,
    safestring,
    termcolors,
    text,
    timesince,
    timezone,
    topological_sort,
    translation,
    tree,
    version,
    xmlutils,
    _os,
    __init__,
)

timezone.now()
datetime_safe.date.today()
datetime_safe.datetime.combine()
datetime_safe.datetime.strftime()
datetime_safe.datetime.strptime()
datetime_safe.strftime()

dateparse
